<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="local.stattaker"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="local.stattaker.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="local.stattaker.FragmentMain"
            android:label="FragmentMain" 
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name="local.stattaker.FragmentPlayerList"
            android:label="FragmentPlayerList"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="local.stattaker.FragmentStats"
            android:label="FragmentStats" 
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name="local.stattaker.CreateTeam"
            android:label="@string/title_activity_create_team" >
        </activity>
        <activity
            android:name="local.stattaker.EditTeam"
            android:label="@string/title_activity_edit" >
        </activity>
    </application>

</manifest>
