<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="local.stattaker"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/qs_logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="local.stattaker.FragmentHolderMain"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="local.stattaker.FragmentHolderWork"
            android:label="FragmentMain"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="local.stattaker.FragmentWorkPlayers"
            android:label="FragmentPlayerList"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="local.stattaker.FragmentWorkStats"
            android:label="FragmentStats"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name="local.stattaker.CreateTeam"
            android:label="@string/title_activity_create_team" >
        </activity>
        <activity
            android:name="local.stattaker.EditTeam"
            android:label="@string/title_activity_edit" >
        </activity>
        <activity
            android:name="local.stattaker.TeamPage"
            android:label="@string/title_activity_team_page" >
        </activity>
    </application>

</manifest>
